import React from 'react';
import styles from './styles.css';

const ShowNext = () => (
  <div className={styles.showNext}>
    <div className={styles.inner}>
      Show 10 next comments
    </div>
  </div>
);

export default ShowNext;
